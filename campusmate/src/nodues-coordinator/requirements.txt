# No-Dues Coordinator — pinned dependencies
# Python 3.12
#
# IMPORTANT: Do NOT use agent-framework==1.6.0 here — it pulls
# agent-framework-core[all] which causes pip ResolutionTooDeep (200k rounds).
# Install agent-framework-core directly (no extras) instead.

# Agent framework — core + foundry only, no [all] extras
agent-framework-core==1.6.0
agent-framework-foundry==1.0.1

# Azure AI Agent Server
azure-ai-agentserver-core==2.0.0b3
azure-ai-agentserver-responses==1.0.0b5

# Azure identity & utilities
azure-identity>=1.19.0,<2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0,<1.0.0

# MCP
mcp>=1.24.0

# Durable orchestration
durabletask>=1.4.0
durabletask-azuremanaged>=1.4.0
agent-framework-durabletask>=1.0.0b260521

# Azure Storage (Teams state persistence)
azure-storage-blob>=12.19.0,<13.0.0

# Approval web server
aiohttp>=3.9.0,<4.0.0

# HTTP client for Teams connector
requests>=2.31.0,<3.0.0

# Tracing
opentelemetry-sdk>=1.20.0
azure-monitor-opentelemetry>=1.0.0
